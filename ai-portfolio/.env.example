# Public GitHub username (defaults to isoqovjorabek2 if unset)
GITHUB_USERNAME=isoqovjorabek2

# Optional: classic PAT or fine-grained token with read access to user data.
# Raises REST rate limits and enables "Contributions (last year)" in stats via GraphQL.
GITHUB_TOKEN=
